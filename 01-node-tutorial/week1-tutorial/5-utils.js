// CommonJS
// Modules
    //Data type: Function.

const sayHi = (name) =>{
    console.log(`Hey there ${name}`);
}

module.exports= sayHi
        //I used =sayHi, bcz I only have 1 function.
        //Here, we're exporting a function: sayHi.